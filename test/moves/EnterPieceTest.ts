import { expect } from 'chai';
import { EnterPiece } from '../../src/moves/EnterPiece';

describe("EnterPiece", function() {
  it('should only be called on a 5 roll');
  it('should be able to bop upon entering');
  it('should not be called if theres a blockade on entry space');
});
